# Configuration

Store versioned dataset, preprocessing, representation, and experiment configurations here.

Configurations should make analyses reproducible across collaborators and machines without embedding local absolute paths or secrets.
