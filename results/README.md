# Results

Commit only small, non-sensitive, publication-relevant derived outputs that can legally be shared.

Suggested structure:

- `tables/`
- `figures/`
- `logs/`

Large arrays, caches, intermediate neural data, and model checkpoints should remain outside Git and are ignored by `.gitignore`.
