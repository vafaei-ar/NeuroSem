# Scripts

Executable workflow entry points belong here.

Planned groups:

- dataset download and integrity checks;
- preprocessing;
- representation extraction;
- RSA and control analyses;
- model-tuning experiments;
- figure/table generation.

Scripts should call reusable code from `src/` and should expose configuration rather than hard-code local paths.
